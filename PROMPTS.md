# AI 협업 기록 / AI Collaboration Log

작성 원칙: 프롬프트 나열이 아니라 **판단 근거**를 남긴다.
Principle: record your **reasoning**, not just prompts.

---

## [이슈 #__] 제목 / Title

**목표(스펙) / Spec**
- 입력 Input:
- 처리 Processing:
- 출력 Output:
- 실패 조건 Failure:

**요청한 프롬프트 요지 / Prompt (summary)**

**결과에 대한 판단 / Decisions**
- 채택한 부분과 이유 / Accepted, because:
- 수정한 부분과 이유 / Changed, because:
- 폐기한 부분과 이유 / Rejected, because:

**검증 방법 / How it was verified**

---
(이슈 단위로 반복 / repeat per issue)

---

## 2026-09-12 · 메모 검색 기능 (2주차 활동) — A조

**지시**
메모 검색 기능 만들어줘. 제목이랑 본문에서 키워드로 찾을 수 있게.

**채택 여부**
미채택.

백엔드 검색 엔드포인트가 아니라 브라우저에서 동작하는 React 화면이 나왔다.
메모를 컴포넌트 상태에 두고 자바스크립트로 필터링하므로 SQL도 라우트도 없고,
`service.js`·`routes.js`에 붙일 지점 자체가 없어 부분 수정이 성립하지 않는다.

또한 코드에 사용자 개념이 없어 `user_id` 조건이 아예 존재하지 않는다.
동작은 하고 에러도 나지 않지만 다른 사용자의 메모까지 검색된다.

**참고**
파일을 하나도 주지 않은 조건. 프로젝트 구조를 스스로 추측했다.
에이전트가 이전 대화에서 얻은 개인의 기술 스택(MySQL·서블릿)을 언급했으므로
완전한 무맥락이 아니라 "프로젝트 맥락 대신 다른 맥락이 있던" 조건에 가깝다.

---

## 2026-09-12 · 메모 검색 기능 (2주차 활동) — B조

**지시**
[지시]
메모 검색 기능을 추가해줘. 제목과 본문에서 키워드로 검색된다.

[규약]
(CONVENTIONS.md 전문 첨부)

[근거]
(schema.sql, service.js, routes.js 전문 첨부)

[종료조건]
- GET /memos/search?q=키워드 로 호출된다
- 제목 또는 본문에 키워드가 포함된 메모만 반환한다
- 본인 메모만 반환한다
- q가 비어 있으면 400과 { ok: false, error } 를 반환한다

**채택 여부**
전체 채택.

`[종료조건]` 네 항목을 모두 만족했다. `WHERE user_id = ?`가 들어갔고
title/body OR 조건이 괄호로 묶여 있어 연산자 우선순위 문제도 없다.
지어낸 컬럼 0개, `CONVENTIONS.md` 위반 0개.

요구하지 않은 두 가지를 스스로 짚었다.
`/memos/search`를 `/memos/:id` 위에 두어야 한다는 라우트 순서 문제와,
검색어의 `%`·`_`를 이스케이프해야 한다는 점이다.

**참고**
결과를 가장 크게 바꾼 자료는 `schema.sql`로 판단했다. 근거는 활동지 4-3 참조.
